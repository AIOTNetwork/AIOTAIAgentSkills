# Do ⭕

- only use styled component on styling
- only import by '@/'
- i18n all texts
- check linting
- extract enum on sibling values
- separate enum / typing on cross file usages
- separate component on reusable logic
- prefer render using map()
- use bunx codegraph < callers > to search files
- prefer const function

# Don't ❌

- avoid '!important'
- avoid 'z-index'
- avoid 'any'
- don't use 'console.log'
- don't comment except TODO or complex logic
- don't export value unless required
