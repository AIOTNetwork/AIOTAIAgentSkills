---
name: tim-research
description: use this when asked to research, explore, or understand something
---

# Codebase 🔑

- when asked about existing code, architecture, conventions
  ✓ use `Glob`, `Grep`, `Read` to explore and understand codebase
- iterate at least 3 times
  ✓ go deeper on each pass
  ✓ start broad (folders, entry points), then narrow (implementations, edge cases)
- write to `research.md`
  ✓ under `Code Findings`
  ✓ use `tim-document` skill
  ✓ architecture, key files, data flow, conventions, gotchas

# Web 🔑

- when asked about library, package, technology, approach
  ✓ use `WebSearch` and `WebFetch` to look up external information
- write to `research.md`
  ✓ under `References`
  ✓ use `tim-document` skill
  ✓ summarize findings (sources and version-specific details)

# Output 🔑

- write `research.md` to `./claude/temp`