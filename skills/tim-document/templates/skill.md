---
name: skill-name
description: use this when asked to {specific trigger action}
---

# {Section} 🔑

- {when / condition}
  ✓ {instruction} on `{file or location}`
- {when / condition}
  ✓ {instruction}

# Example 🔑

- when asked about existing code, architecture, conventions
  ✓ use `Glob`, `Grep`, `Read` to explore and understand codebase
- iterate at least 3 times
  ✓ go deeper on each pass
  ✓ start broad (folders, entry points), then narrow (implementations, edge cases)
- write to `research.md`
  ✓ under `Code Findings`
  ✓ architecture, key files, data flow, conventions, gotchas